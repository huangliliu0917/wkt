<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zmj.wkt.mapper.RegionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zmj.wkt.entity.Region">
        <result column="id" property="id" />
        <result column="parent_id" property="parent_id" />
        <result column="region_name" property="region_name" />
        <result column="region_type" property="region_type" />
        <result column="agency_id" property="agency_id" />
        <result column="areaid" property="areaid" />
        <result column="zip" property="zip" />
        <result column="code" property="code" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, parent_id, region_name, region_type, agency_id, areaid, zip, code
    </sql>

</mapper>
